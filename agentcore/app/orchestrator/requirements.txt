# Governed Companion — AgentCore Runtime app deps (/invocations face), deploy-time.
# The engine source is COPYied into the image (not pip-installed). Pins are current PATCHED
# floors (watch advisories and bump).

# AgentCore Runtime + observability
bedrock-agentcore>=1.18.1   # CVE-2026-16796 fixed in 1.18.1
aws-opentelemetry-distro>=0.17.1   # floor set (was unpinned); resolve to an exact version at prod image build
opentelemetry-api>=1.20.0

# Inbound identity (defense-in-depth behind the platform authorizer), guarded import
PyJWT[crypto]>=2.13.0       # CVE-2026-32597 / -48526 / PyJWKClient SSRF-DoS fixed by 2.13.0

# Engine runtime deps (mirror the repo-root requirements)
pyyaml>=6.0
boto3>=1.34.0               # S3 KB store/overlay, Bedrock (Tier 3)
