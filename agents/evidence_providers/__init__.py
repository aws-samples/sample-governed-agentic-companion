"""Optional evidence providers for the evidence gateway (see ../evidence_gateway.py).

Each provider implements the EvidenceProvider protocol. The generic gateway contract has
no dependency on any provider in this package.
"""
